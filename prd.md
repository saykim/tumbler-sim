드럼, 텀블러에 들어있는 냉동치킨, 너겟들이 잘 섞이는지 시뮬레이션하는 프로그램을 아래 PRD로 만들거야.

가장 최적의 프로젝 구조와 라이브러리는??

# PRD: Rotating Drum + Baffle Mixing Simulator (2D) — Single File HTML

## 1. 한 줄 요약(One-liner)

회전 드럼(텀블러) 내부에서 너겟/펠릿 유사 조각이 배플(Baffle)에 의해 얼마나 잘 뒤집히고 섞이는지, **설계 파라미터(RPM/배플 개수·각도·길이 등)** 별로 **정량 지표로 비교**하는 브라우저 실행형(설치 불필요) 2D 시뮬레이터.

---

## 2. 목적(Goals)

### 2.1 제품 목표

- **배플 설계안 비교**: 배플 개수/길이/각도/위치(향후) 조합별로 혼합/뒤집힘 경향을 빠르게 비교
- **공정 조건 탐색**: 드럼 RPM, 중력 스케일(g), 마찰/탄성 변화에 따른 레짐 변화 확인
- **대화/의사결정 지원**: 엔지니어·기계설계·현장 운영 간 설계 토론을 “눈으로 보고 수치로” 정렬

### 2.2 비목표(Non-goals)

- 3D 정밀 검증(DEM급 과립물리) 대체
- 실제 제품 형상(불규칙 폴리곤/변형체) 및 열/습도/점착(adhesion) 등 복잡 물성까지 재현
- 규제/품질 인증용 “검증된 물리모델” 제공

---

## 3. 사용자 및 사용 시나리오

### 3.1 핵심 사용자

- 기계 설계자(배플/드럼 구조 설계)
- 공정 엔지니어(혼합/뒤집힘 성능 검토)
- 제조/품질 운영(변경 전후 영향 평가)

### 3.2 대표 시나리오

1. “배플 4개 vs 6개”에서 뒤집힘 증가가 있는지 확인
2. 배플 각도(+/-)에 따른 혼합도 변화 비교
3. RPM 상향 시 캐스케이딩(cascading) → 카타락팅(cataracting) 전환 경향 관찰(2D 근사)
4. 마찰/탄성 변화(제품 코팅/표면상태 변화) 가정하여 성능 민감도 확인

---

## 4. 핵심 지표(KPIs) 정의

> **중요**: 이 시뮬레이터의 “성공”은 물리적 절대 정확도보다 **설계안 간 상대 비교의 일관성**이다.

### 4.1 Mixing Index (0~1)

- 정의(기본): 드럼 중심 기준 **각도(θ) 구간**으로 빈(bin)을 나누고, A/B 라벨(초기 반반)을 배치한 뒤
  - 각 bin의 A 비율이 전체 A 비율과 얼마나 가까운지 측정
  - 값이 1에 가까울수록 혼합이 잘 된 상태
- 요구: 실시간 표시, 파라미터 변경 시 추세 비교 가능

### 4.2 Flip Rate (flips/min/particle)

- 정의(기본): 각 입자의 회전각(orientation)을 추적하고 **π 라디안(half-turn)** 이상 누적 회전 시 flip 카운트
- 표시: 분당/입자당 뒤집힘 빈도

### 4.3 Performance

- FPS 30 이상(기본 목표), Particle Count 200 기준에서 체감 프레임 드랍이 과도하지 않을 것

---

## 5. 범위(Scope)

### 5.1 MVP 범위(필수)

- 단일 파일(HTML+CSS+JS)로 실행(설치 과정 없음)
- 2D 물리 엔진 기반(예: Matter.js CDN 로드)
- 드럼(원형 경계) + 배플(내부 고정/드럼에 부착된 형태) + 입자(너겟 근사 강체)
- 실시간 컨트롤 패널:
  - Drum RPM
  - Gravity 스케일(g)
  - Particle Count / Size
  - Baffle Count / Length / Tilt
  - Friction / Restitution
- 지표 표시:
  - Mixing Index
  - Flip Rate
  - FPS, Particle Count
- Reset/Rebuild, Randomize 기능

### 5.2 v1 확장(권장)

- Baffle “위치 오프셋/패턴” (예: 한쪽 편향 배치, 비대칭 배치)
- 입자 분포(크기 분포, 2~3종 혼합)
- 혼합 지표 추가:
  - 반경(r) 구간 기반 균일도(중심/벽 근처 분리)
  - 체류시간(Residence time) 근사(가상의 출구/구역 통과 빈도)
- 결과 내보내기:
  - CSV(시간 vs 지표)
  - 설정 프리셋 저장/불러오기(JSON)

### 5.3 v2(고도화)

- 3D 전환(별도 프로젝트): Rapier(3D) + three.js 또는 Python(PyBullet) 기반 검증 파이프라인
- 실제 너겟 형상 근사(복합구/다각형 클럼프), 점착/응집(cohesion) 모델(제한적으로)

---

## 6. 기능 요구사항(Functional Requirements)

### FR-01 단일 파일 실행

- 사용자는 파일을 `.html`로 저장 후 브라우저에서 열기만 하면 실행 가능해야 한다.
- 외부 의존성은 CDN 로드만 허용(옵션: 완전 오프라인 번들 버전은 v1.5로 별도)

### FR-02 물리 시뮬레이션 루프

- `requestAnimationFrame` 기반 렌더링
- 물리 엔진 스텝과 렌더링의 안정적 동기화(엔진 Runner 사용 또는 고정 time step)

### FR-03 파라미터 제어

- 슬라이더 조작 즉시 반영되는 항목(RPM, g, 마찰/탄성)
- 구조 변경이 필요한 항목은 Reset/Rebuild로 반영(입자 수, 배플 수 등)

### FR-04 지표 계산/표시

- 매 프레임 또는 일정 주기(예: 200~500ms)로 지표 업데이트
- 지표는 화면에 항상 노출(컨트롤 패널 KPI 카드)

### FR-05 재현성/비교 가능성

- Randomize 버튼으로 “초기 분포 랜덤화” 제공
- (v1 확장) 난수 시드(seed) 고정 옵션 제공

---

## 7. 비기능 요구사항(Non-Functional Requirements)

### NFR-01 성능

- Particle 200 기준 FPS 30 이상(권장), 300까지 “사용 가능” 수준 유지
- 과도한 충돌로 불안정해지지 않도록 엔진 파라미터(Iterations) 적정 조정 가능해야 함

### NFR-02 안정성

- 초기화/리셋 중 예외 발생 시 사용자에게 오류 메시지 제공
- 리사이즈 시 레이아웃/시뮬레이션이 깨지지 않도록 재빌드 처리

### NFR-03 가독성/유지보수

- 전역 상태 객체(state)로 파라미터/월드/지표를 구조화
- 함수 단위 분리(드럼 생성, 입자 생성, 모터, 지표 계산, UI 바인딩)

---

## 8. 데이터/모델링 가정(Assumptions)

- 입자는 “너겟 유사”를 **둥근 사각형(rounded rectangle)** 강체로 근사
- 배플은 드럼에 고정된 강체로 모델링(드럼 회전과 함께 회전)
- 2D 단면 근사이므로 3D 현상(상하층 흐름, 실제 체적 충돌)은 반영되지 않음
- 절대값 예측보다는 **설계안 상대 비교**에 초점을 둔다

---

## 9. 리스크 및 대응

### R-01 2D 근사의 한계

- 리스크: 실제 3D 혼합과 결과가 다를 수 있음
- 대응: “설계 탐색/경향성”으로 위치 명확화 + 유망 케이스를 3D(PyBullet/DEM)로 2차 검증

### R-02 물리 엔진 안정성(고밀도 입자)

- 리스크: 입자 수 증가 시 폭발/관통/떨림
- 대응: 스폰 시 오버랩 최소화, 마찰/탄성 범위 제한, 엔진 iterations 옵션 제공(v1 확장)

### R-03 지표의 타당성

- 리스크: 혼합도를 한 가지 지표로 단정
- 대응: v1에서 지표 2~3개로 확장(각도 기반 + 반경 기반 + 시간 평균)

---

## 10. 수용 기준(Acceptance Criteria)

- AC-01: 단일 HTML 파일을 브라우저로 열면 즉시 실행된다.
- AC-02: RPM/배플/입자 파라미터를 변경하면, Reset 후 시뮬레이션과 지표가 일관되게 변화한다.
- AC-03: Mixing Index와 Flip Rate가 실시간으로 표시되고, 설계안 비교가 가능하다.
- AC-04: Particle 200 기준에서 시뮬레이션이 “실사용 가능”한 프레임(>=30 FPS)에 근접한다(환경에 따라 변동 가능).

---

## 11. 향후 연동(옵션)

- Python 검증 파이프라인(별도):
  - PyBullet 기반 3D PoC로 상위 검증
  - DEM(Chrono/YADE) 기반 정밀 검증
- 결과 리포트(자동 생성):
  - 프리셋별 지표 비교 테이블/그래프(HTML 내 export 또는 Python 분석)

---
