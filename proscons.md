# Self-Reflection: 드럼 믹싱 시뮬레이터 설계 검토

---

## 1. Pros (장점)

### 1.1 아키텍처 측면

**단일 파일 구조의 실용성**
PRD 요구사항인 "설치 없이 브라우저에서 즉시 실행"을 완벽히 충족합니다. 엔지니어나 현장 담당자가 이메일로 파일 하나만 받아서 바로 테스트할 수 있어 배포와 공유가 매우 용이합니다.

**Matter.js 선택의 적절성**
2D 물리 시뮬레이션에서 Matter.js는 문서화가 잘 되어 있고, Composite 기능으로 드럼+배플 일체 회전 구현이 자연스럽습니다. 200개 입자 기준 30FPS 목표 달성에 적합한 성능을 제공합니다.

**모듈화된 코드 구조**
단일 파일이지만 CONFIG, STATE, FlipTracker 등 논리적 섹션 분리가 명확하여 유지보수성이 확보됩니다. 특정 기능 수정 시 해당 섹션만 찾아가면 됩니다.

### 1.2 기능 측면

**포괄적인 파라미터 커버리지**
드럼 크기/RPM, 배플 개수/길이/각도/위치, 재료 수량/크기/물성 등 PRD에서 요구한 모든 조정 가능 파라미터를 포함했습니다.

**다차원 스코어링 시스템**
단일 지표가 아닌 4개 지표(Flip, Mixing, Coverage, Motion)의 가중 평균으로 종합 점수를 산출하여, "뒤집힘이 좋지만 혼합이 안 되는" 등의 복합적 상황을 구분할 수 있습니다.

**실시간 피드백 루프**
슬라이더 조작 → 즉시 시뮬레이션 반영 → 스코어 업데이트 → 개선 제안까지 사용자가 직관적으로 설계를 탐색할 수 있습니다.

### 1.3 UX 측면

**시각적 대시보드**
Chart.js 시계열 그래프로 시간에 따른 지표 변화를 추적할 수 있어, "초반에는 혼합이 안 되다가 1분 후부터 안정화" 같은 패턴을 파악할 수 있습니다.

**등급 시스템과 제안**
S/A/B/C/D/F 등급과 함께 "RPM 감소 권장" 같은 구체적 제안을 제공하여 비전문가도 방향성을 잡을 수 있습니다.

---

## 2. Cons (단점 및 한계)

### 2.1 물리 시뮬레이션의 한계

**2D 근사의 본질적 오차**
실제 드럼은 3D이고, 입자가 축 방향(깊이)으로도 이동합니다. 2D에서는 이 현상을 전혀 반영하지 못하므로, 실제 혼합 효율과 시뮬레이션 결과 사이에 상당한 괴리가 발생할 수 있습니다. 특히 나선형 배플이나 경사 드럼 같은 3D 특성이 중요한 설계는 평가 불가능합니다.

**입자 형상 단순화**
실제 너겟은 불규칙한 형상이고 서로 맞물리거나 끼는 현상이 있습니다. 둥근 사각형(rounded rectangle)으로 근사하면 실제보다 훨씬 잘 굴러가고 분리됩니다.

**점착/응집 미반영**
냉동 식품은 표면 결빙, 기름기 등으로 서로 달라붙는 경우가 많은데, 이 점착(adhesion/cohesion) 물성이 전혀 없습니다.

### 2.2 스코어링 시스템의 문제점

**경험적 기준값의 근거 부족**
"최적 flip rate = 15회/분"이라는 기준이 실제 공정 데이터가 아닌 임의 설정입니다. 실제 현장에서 "좋은 뒤집힘"이 몇 회인지 검증되지 않았습니다.

**가중치의 자의성**
Flip 35%, Mixing 30%, Coverage 20%, Motion 15%라는 가중치가 어떤 근거로 설정되었는지 불명확합니다. 공정 목적에 따라 가중치가 완전히 달라져야 할 수 있습니다.

**Mixing Index 계산의 한계**
θ-bin 방식은 "좌우 절반 분리"는 잘 감지하지만, "중심부 vs 벽면" 분리(반경 방향 편석)는 놓칩니다. 입자가 원심력으로 벽에 몰리는 현상이 있어도 Mixing Index가 높게 나올 수 있습니다.

### 2.3 성능 및 확장성

**입자 수 제한**
Matter.js로 300개 이상에서는 프레임 드랍이 심해집니다. 실제 드럼에는 수천 개의 너겟이 들어가므로, 스케일 면에서 현실과 동떨어져 있습니다.

**단일 파일의 유지보수 한계**
현재 약 800-1000줄인데, v1 확장 기능(프리셋 관리, 다중 시나리오 비교, 상세 리포트)을 추가하면 2000줄 이상으로 커져서 단일 파일 관리가 어려워집니다.

### 2.4 검증 부재

**실측 데이터와의 비교 없음**
시뮬레이터의 스코어가 실제 혼합 품질과 어느 정도 상관관계가 있는지 전혀 검증되지 않았습니다. "시뮬레이터에서 S등급이면 실제로도 좋다"는 보장이 없습니다.

**재현성 문제**
동일 파라미터로 시뮬레이션을 재실행해도 초기 입자 위치가 랜덤이라 결과가 달라집니다. 시드 고정 옵션이 구현되어 있지 않아 정확한 비교 실험이 어렵습니다.

---

## 3. 개선 제안

### 3.1 즉시 적용 가능 (Low-effort, High-impact)

**반경 방향 Mixing Index 추가**
기존 θ-bin에 더해 r-bin(중심/중간/벽면 3구간)으로 반경 방향 균일도도 측정하면 원심 분리 현상을 감지할 수 있습니다.

```javascript
function calcRadialMixing() {
  const bins = [
    { a: 0, b: 0 }, // 중심부 (r < 33%)
    { a: 0, b: 0 }, // 중간부 (33% < r < 66%)
    { a: 0, b: 0 }, // 외곽부 (r > 66%)
  ];
  // ... 반경 기준 분류 로직
}
```

**난수 시드 고정 옵션**
같은 시드로 여러 배플 설계를 비교할 수 있게 하여 재현성 확보가 필요합니다.

**가중치 사용자 조정**
스코어 가중치를 슬라이더로 조정 가능하게 하여, 사용자가 "우리 공정은 뒤집힘이 더 중요해"라고 반영할 수 있게 해야 합니다.

### 3.2 중기 개선 (v1.5)

**다중 시나리오 비교 모드**
설정 A, B, C를 저장해두고 동시에 돌려서 표로 비교하는 기능이 필요합니다. 현재는 한 번에 하나씩만 테스트할 수 있어 비효율적입니다.

**실측 데이터 입력 및 캘리브레이션**
실제 공정에서 "이 설정이 좋았다/나빴다" 데이터를 입력받아 스코어링 기준을 조정하는 학습 기능을 고려할 수 있습니다.

### 3.3 장기 개선 (v2)

**3D 전환**
PRD에서도 언급된 대로, 유망 설계안은 Three.js + Rapier 또는 Python PyBullet으로 3D 검증이 필요합니다. 2D 시뮬레이터는 빠른 탐색용으로만 포지셔닝해야 합니다.

**DEM 연계**
최종 검증은 EDEM, Rocky 같은 상용 DEM 소프트웨어나 오픈소스 LIGGGHTS로 수행하고, 이 시뮬레이터는 그 전 단계 스크리닝 도구로 역할을 명확히 해야 합니다.

---

## 4. 종합 평가

| 항목              | 점수   | 코멘트                                    |
| ----------------- | ------ | ----------------------------------------- |
| PRD 요구사항 충족 | 85/100 | MVP 범위는 충족, v1 확장 일부 미구현      |
| 물리적 타당성     | 50/100 | 2D 근사 + 단순 형상의 본질적 한계         |
| 스코어링 신뢰성   | 40/100 | 검증 없는 경험적 기준, 개선 필요          |
| 사용성/UX         | 90/100 | 직관적 UI, 실시간 피드백 우수             |
| 확장성            | 60/100 | 단일 파일 한계, 모듈 분리 필요 시점 도래  |
| 실무 적용 가능성  | 65/100 | "설계 탐색/경향성 파악"으로 한정하면 유용 |

### 핵심 결론

이 시뮬레이터는 **"정밀 예측 도구"가 아니라 "설계 탐색 및 의사소통 도구"**로 포지셔닝해야 합니다. 사용자에게 "이 스코어는 상대 비교용이며, 절대적 품질 예측이 아님"을 명확히 고지하고, 유망 설계안은 반드시 실물 테스트나 3D 시뮬레이션으로 2차 검증하는 프로세스를 권장해야 합니다.

스코어링 시스템은 현재 "있으면 좋은" 수준이고, 실측 데이터 기반 캘리브레이션 없이는 신뢰하기 어렵습니다. v1에서 가중치 조정 기능과 반경 방향 지표를 추가하고, 실제 공정 결과와 비교하여 기준값을 튜닝하는 과정이 필수적입니다.
